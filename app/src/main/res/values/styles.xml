<resources>
    <style name="Theme.YourApp" parent="Theme.MaterialComponents.DayNight">
        <!-- Override the default BottomNavigationView style -->
        <item name="bottomNavigationStyle">@style/CustomBottomNavigationView</item>
    </style>

    <style name="CustomBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <!-- Disable the active indicator -->
        <item name="itemActiveIndicatorStyle">@null</item>
    </style>

</resources>

